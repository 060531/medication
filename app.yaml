name: Build and Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Build Project
        # ใช้ app.py สำหรับการ build
        run: |
          python app.py  # หรือคำสั่งที่จำเป็นสำหรับการรัน app.py ของคุณ

      - name: ตรวจสอบ Directory ของผลลัพธ์
        run: |
          echo "Contents of output_dir:"
          ls -R ./output_dir

      - name: Disable Jekyll
        # เพิ่ม .nojekyll เพื่อปิดการใช้งาน Jekyll
        run: echo "" > ./output_dir/.nojekyll

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./output_dir  # ระบุ Directory ของผลลัพธ์ build ที่ถูกต้อง
